<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Quote by <%= quote.author %></title>
    <link rel="stylesheet" href="/stylesheets/show.css" />
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <div class="show-body">
      <div class="show-header">
        <h1>"<%= quote.quote %>"</h1>
        <p>— <%= quote.author %></p>
      </div>
      <div class="show-sidebar">
        <div class="show-actions">
          <a href="/users/<%= user._id %>/quotes/<%= quote._id %>/edit">
            Edit this quote
          </a>
          <form
            action="/users/<%= user._id %>/quotes/<%= quote._id %>?_method=DELETE"
            method="POST"
          >
            <button type="submit">Delete this quote</button>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
